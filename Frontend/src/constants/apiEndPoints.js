const BACKEND_URL = 'http://localhost:3000';

const REGISTER = {
  url: 'user',
  method: 'POST',
};

const LOGIN = {
  url: 'login',
  method: 'POST',
};

const TOKEN_VALIDATE = {
  url: 'token/validate',
  method: 'GET',
};
